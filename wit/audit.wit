package fractal:audit@0.1.0;

interface log {
    record-event: func(entry: audit-entry);

    record audit-entry {
        event-type: string,
        resource: string,
        detail: string,
    }
}
